# Operations

The sandbox exposes lightweight operational endpoints:

- `GET /health` returns service identity and status.
- `GET /health/ready` returns in-memory readiness counts.
- `GET /metadata` returns read-only capability metadata.
